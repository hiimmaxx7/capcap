# PROGRESS.md — Capcap

Nhật ký phát triển. Đọc file này trước khi đổi hành vi quay/UI để biết quyết định nào đã chốt và vì sao.

## Kiến trúc hiện tại

- **.NET Framework 4.8** (đổi từ .NET 8 — xem lý do bên dưới), WinForms, chạy dưới dạng tray icon (`TrayApplicationContext`), không cửa sổ chính.
- Quay màn hình: `Recorder` chụp từng khung hình qua **DXGI Desktop Duplication** (`DxgiScreenCapture`, GPU-accelerated) — tự động lùi về GDI `CopyFromScreen` nếu máy không khởi tạo được DXGI cho màn hình đó. Vẽ con trỏ thật lên (`CursorPainter`) rồi pipe raw frame vào `ffmpeg` qua stdin để encode H.264 (`libx264 -preset ultrafast -tune zerolatency`). Pixel format: `bgra` khi dùng DXGI (khớp định dạng gốc B8G8R8A8 của desktop, không cần convert), `bgr24` khi dùng GDI.
- Kiến trúc producer/consumer: luồng chụp (`CaptureLoop`) và luồng ghi vào ffmpeg (`WriterLoop`) tách biệt qua `BlockingCollection<byte[]>` + buffer pool — encode chậm không được phép làm treo nhịp chụp.
- Âm thanh hiệu ứng (click/phím/cuộn) không phải thu mic — chỉ log timestamp qua low-level hook (`InputSoundLogger`) rồi trộn các file WAV thu sẵn (`Sounds\*.wav`, nhúng vào exe, giải mã ở `InputSoundBank`) vào đúng các mốc đó (`WavBuilder`, ghi theo từng khối 1 giây để không giữ cả track trong RAM).
- Âm thanh hệ thống (tùy chọn, tắt mặc định): `SystemAudioCapture` dùng NAudio `WasapiLoopbackCapture`, ghi ra WAV riêng.
- Mux cuối cùng: 0, 1 hoặc 2 track âm thanh (hiệu ứng + hệ thống) được gộp vào video bằng một lệnh ffmpeg pass thứ 2 — nếu có 2 track thì dùng `-filter_complex amix` để trộn, không chỉ giữ lại track đầu.

## Các quyết định đã chốt (đừng đổi lại nếu không có lý do mới)

- **Hotkey: `Ctrl+Home` (bắt đầu) / `Ctrl+End` (dừng) / `Ctrl+P` (tạm dừng)**, đăng ký qua `RegisterHotKey`. Đã thử double-tap Home/End và `Ctrl+Alt+R` trước đó — bỏ vì `Ctrl+Alt+R` bị xung đột với AutoHotkey/EVKey trên máy người dùng (không tự phát hiện được lỗi này, hotkey chỉ âm thầm không bắn). Bấm đúp icon khay hệ thống luôn là phương án dự phòng không phụ thuộc hotkey.
- **Chặn chạy nhiều instance** (`Mutex` trong `Program.cs`) — lý do: 2 tiến trình cùng chạy khiến chỉ 1 cái đăng ký được hotkey, gây cảm giác "đổi cài đặt không có tác dụng" rất khó debug.
- **FPS mặc định 60**, chỉ còn 2 lựa chọn 30/60 (đã bỏ 10/15/24 theo yêu cầu).
- **Âm thanh click/phím/cuộn: bật mặc định**, dùng 3 file WAV thật người dùng cung cấp (thay cho sóng sin tổng hợp trước đây — 2026-09-25): `mouse click.wav`, `typing 2.wav`, `mouse scroll.WAV` (gốc ở `D:\materials`, copy vào `Sounds\` thành `click/typing/scroll.wav`). Cắt khoảng lặng đầu file để tiếng khớp đúng lúc bấm. `typing 2.wav` là cả tràng ~15 lần gõ nên được tự cắt thành từng phím (dò onset theo đường bao 10ms) và phát xoay vòng ngẫu nhiên, không lặp 1 phím 2 lần liền. File scroll đã chứa vài tiếng tách nên khi lăn liên tục chỉ phát lại mỗi ≥0.15s, không chồng lên nhau. Âm lượng chuẩn hóa: click đỉnh 0.75, phím/cuộn 0.6.
- **Cỡ con trỏ: cố định 2x, đã BỎ menu chọn cỡ (2026-09-25)** — người dùng thấy cỡ hiện tại (2x → vẽ ở cỡ 72px có sẵn trong file .cur) là đẹp và yêu cầu bỏ tùy chọn 1x/1.5x/2x/3x. Giá trị nằm ở `RecordingOptions.CursorScale`. Lịch sử: ban đầu 2x (không phải kích thước gốc OS). Lý do: có cố gắng tự tính đúng kích thước theo DPI màn hình (`GetSystemMetricsForDpi`) nhưng không kiểm chứng được trên máy người dùng là có tác dụng hay không, nên thêm hẳn tùy chọn nhân hệ số thủ công (1x/1.5x/2x/3x) làm phương án chắc chắn có hiệu quả.
- **Con trỏ nét khi phóng to (2026-09-25)**: KHÔNG kéo giãn bitmap của handle con trỏ. `DrawIconEx` kéo giãn thì bị răng cưa (người dùng chụp ảnh so sánh), còn `Icon.ToBitmap` + bicubic thì bị mờ — một thay đổi chưa commit ngày 21/9 đã thử cách này và bị người dùng chê, đã bỏ. Cách đang dùng: `CursorPainter.PrepareSharpCursors` đọc file .cur của bộ con trỏ hiện tại (`HKCU\Control Panel\Cursors`, máy người dùng dùng bộ `*_eoa.cur` theo Pointer size = 48, file chứa sẵn 48/72/96/144/192px), chọn cỡ có sẵn gần cỡ cần vẽ nhất rồi vẽ 1:1. Vì vậy cỡ vẽ ra có thể lệch một chút so với cỡ tính toán (ví dụ 2x tính ra 64 thì vẽ 72). Con trỏ riêng do app tự tạo không có trong bộ con trỏ nên vẫn bị kéo giãn như cũ.
- **Dead-zone cho chế độ Dọc 9:16 bám con trỏ**: chỉ pan khi con trỏ vào trong khoảng 1/6 bề rộng khung tính từ mép trái/phải; ở giữa đứng yên hoàn toàn. Khi pan thì dùng easing kết hợp: hệ số tỉ lệ (êm khi gần đích) + **sàn tốc độ tối thiểu** (đóng hết 1 chiều rộng khung trong ≤0.3s) để không bị "ì" khi con trỏ nhảy xa — đã tự chỉnh 2 lần vì lần đầu (easing thuần, tau=0.4s) quá chậm bắt kịp khi con trỏ trượt hẳn ra ngoài khung.
- **Viền báo vùng đang quay**: dùng `SetWindowDisplayAffinity(WDA_EXCLUDEFROMCAPTURE)` để loại cửa sổ viền khỏi mọi kiểu chụp màn hình — KHÔNG dùng mẹo vẽ lệch ra ngoài vài px (đã thử, vẫn lọt vào video khi viền phải di chuyển nhanh theo chế độ bám chuột).
- **Đồng bộ âm thanh với video**: hai nguồn lệch đã tìm ra và sửa:
  1. Độ trễ khởi động (ffmpeg + thread spin-up) trước khung hình đầu tiên → bù bằng `_firstFrameOffsetSec`, dịch toàn bộ mốc thời gian sự kiện âm thanh theo offset này.
  2. Khi máy chụp không kịp fps mục tiêu, code cũ **âm thầm bỏ khung hình** → video bị nén ngắn hơn thời lượng quay thật (giống tua nhanh), làm âm thanh lệch dần theo thời gian. Đã sửa bằng cách **lặp lại khung hình gần nhất** để bù đủ số khung cần có, đảm bảo thời lượng video luôn khớp thời gian thực (đã đo: quay 4.72s thực tế ra video 4.23s, so với trước khi sửa 3.2s thực tế chỉ ra 0.9s video).
- **Popup Lưu/Xóa sau khi dừng quay** — luôn hỏi trước khi giữ file lại trong `Videos\capcap`.
- **Tên dự án: Capcap** (đổi từ Project Oculus) — chơi chữ "cap" = capture, lặp "cap cap" nghe như tiếng vịt kêu "cạp cạp", khớp với icon Psyduck đang dùng.
- **Đếm ngược 3-2-1 áp dụng cho MỌI chế độ** (đổi 2026-09-25 theo yêu cầu người dùng; trước đó chỉ có ở "Chọn vùng"). Số đếm hiện giữa vùng sắp quay (vùng đã chọn / dải 9:16 quanh con trỏ / giữa màn hình). Trong lúc đếm, `Ctrl+End` hoặc bấm đúp icon khay = hủy đếm; bấm `Ctrl+Home` lần nữa không tạo đếm ngược thứ 2.
- **Nhớ vùng chọn cuối** (2026-09-25): lưu ở `%AppData%\Capcap\settings.ini` (`LastRegion=x,y,w,h`), sống qua các lần mở app. Lý do: người dùng hay quay dọc 608x1080 và phải kéo lại mỗi lần. Khi mở màn chọn vùng: khung cũ (viền vàng nét đứt) chạy theo con trỏ — click = dùng khung đó tại chỗ, Enter = đúng vị trí cũ, kéo = vẽ mới, giữ Shift khi kéo = khóa tỉ lệ cũ.
- **Đổi target framework sang net48** — .NET Framework 4.8 có sẵn trên mọi máy Windows 10/11, người dùng không cần cài .NET Desktop Runtime riêng như .NET 8 trước đây. Đánh đổi: phải tự thay `Application.SetHighDpiMode` (không tồn tại ở .NET Framework WinForms) bằng khai báo `dpiAwareness` trong `app.manifest`; `Math.Clamp` không có ở net48 (dùng Min/Max thủ công); literal `"..."u8` cần `Span<byte>` nên đổi sang `Encoding.ASCII.GetBytes`; so sánh `IntPtr == int` (wParam trong hook) cần `.ToInt32()` tường minh vì .NET Framework không có operator tiện lợi như .NET 8.
- **Tên file video: `capcap_yyyyMMdd_HHmmss.mp4`** (đổi từ `ghi_...`).
- **Ghi âm thanh hệ thống dùng NAudio** (`WasapiLoopbackCapture`), không tự viết WASAPI qua P/Invoke — đánh đổi chấp nhận được: build không còn ra đúng 1 file .exe nữa mà kèm ~10 DLL nhỏ của NAudio (~1MB tổng), nhưng tránh rủi ro tự implement COM interop WASAPI (rất dễ sai định dạng/timing). Chưa test được nội dung âm thanh thực tế trong sandbox này vì **máy test không có thiết bị phát âm thanh nào** (`GetDefaultAudioEndpoint` báo lỗi "Element not found") — đã thêm thông báo lỗi tiếng Việt rõ ràng cho đúng trường hợp này, nhưng cần người dùng tự xác nhận tính năng hoạt động đúng trên máy thật của họ.

- **DXGI Desktop Duplication thay GDI `CopyFromScreen`** — đã triển khai sau khi người dùng xác nhận full màn hình 60fps vẫn giật dù đã tách luồng capture/encode. **Sửa lại một hiểu nhầm trước đó**: từng cho rằng máy test là sandbox ảo hóa nên GDI capture chậm bất thường — thực ra đó là máy thật của người dùng, và GDI `CopyFromScreen` thật sự chỉ đạt ~17fps thực tế khi chụp full 1920x1080 ở 60fps mục tiêu trên máy đó. Đổi sang DXGI đo được ~56-60fps thực tế (gần đúng tuyệt đối target), 9:16 crop nhỏ đạt đúng 60fps tuyệt đối không rớt khung nào. Dùng gói NuGet `Vortice.DXGI`/`Vortice.Direct3D11` bản **1.6.0** (không phải bản mới nhất 3.x) vì bản mới chỉ target net7.0/net8.0, không tương thích net48 — 1.6.0 target `netstandard2.0` nên dùng được. Cursor vẫn vẽ qua `CursorPainter` cũ y nguyên bằng cách pin buffer BGRA thành `Bitmap` tạm (zero-copy) rồi vẽ GDI lên đó — không viết lại logic vẽ cursor cho D3D.
  - **ĐÃ SỬA (2026-09-25) — crash khi bấm Ctrl+End**: Event Log xác nhận app chết vì `AccessViolationException` trong `d3d11.dll` tại `DxgiScreenCapture.Dispose()`. Nguyên nhân thật (không phải STA/message pump như suy đoán bên dưới): Vortice 1.6 cache các object trả về từ `factory.Adapters1` / `adapter.Outputs` và tự `Release()` chúng khi factory bị dispose — code cũ lại `Dispose()` adapter thêm lần nữa → release thừa 1 lần → D3D11 truy cập adapter đã giải phóng khi hủy device (ngẫu nhiên tùy refcount nội bộ, nên "thỉnh thoảng"). Sửa: lấy tham chiếu riêng bằng `QueryInterface` và giữ factory sống tới khi tạo xong device/duplication. Kèm theo: box copy đổi sang tọa độ cục bộ của màn hình + không bao giờ copy ngoài biên, luôn `ReleaseFrame` trong `finally`, chế độ Chọn vùng quay trên màn hình chứa vùng (không phải màn hình chứa chuột). Kiểm chứng: harness 60 vòng tạo/chụp/hủy — bản cũ crash đúng stack trace trên 1/3 lần chạy, bản mới 8/8 lần sạch.
  - (Ghi chú cũ) **Lưu ý rủi ro đã phát hiện**: một test harness độc lập (không có vòng lặp message pump WinForms) bị `AccessViolationException` khi giải phóng COM object của DXGI lúc `Dispose()`. Đã test lại bằng app thật (luôn có `Application.Run` message pump) với 1 lần và sau đó 5 lần quay/dừng liên tiếp — không crash lần nào. Nghi ngờ nguyên nhân là do STA COM cần message pump để release đúng cách, môi trường thật của app luôn có pump nên an toàn — nhưng đây là **suy luận, chưa chắc chắn 100%**, cần để ý nếu người dùng báo app tự thoát/crash sau khi dừng quay.
- **Giới hạn thời lượng quay tối đa (auto-stop sau N giây)**: từng hiểu nhầm yêu cầu "mặc định 30s" theo hướng này — thực ra ý người dùng là "mặc định 30fps" (đã hỏi lại và xác nhận). Không có tính năng giới hạn thời lượng.
- **Repo public + GitHub Release**: đã đổi repo `capcap` sang public, đóng gói `bin\Release\net48\*` (trừ .pdb) thành zip đính kèm GitHub Release để người khác tải về dùng ngay không cần build.

## Việc còn để ngỏ / có thể cần theo dõi tiếp

- Ctrl+Home/Ctrl+End/Ctrl+P chiếm dụng phím tắt toàn hệ thống (trùng chức năng mặc định ở nhiều app khác: về đầu/cuối văn bản, In). Người dùng đã được nhắc, chưa yêu cầu đổi.
- **Ghi âm thanh hệ thống chưa test được nội dung thực tế** — máy test hiện không có thiết bị phát âm thanh cấu hình mặc định (`GetDefaultAudioEndpoint` báo lỗi "Element not found", dù đây là máy thật) — đã thêm thông báo lỗi tiếng Việt rõ ràng cho đúng trường hợp này, nhưng cần người dùng tự xác nhận tính năng hoạt động đúng khi có thiết bị âm thanh.
- Nếu máy người dùng không có GPU/driver hỗ trợ DXGI Desktop Duplication, code tự lùi về GDI như cũ (chậm hơn nhưng vẫn chạy được) — chưa có cách nào báo cho người dùng biết đang chạy ở chế độ nào (DXGI hay GDI fallback).
